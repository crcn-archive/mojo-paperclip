all: clean lib test-web;

clean:
	rm -rf test-web;
	rm -rf lib;


lib:
	coffee -b -o lib -c src;
	
lib-watch:
	coffee -b -o lib -cw src;

test-web:
	cp -r test test-web;
	for F in `ls test-web | grep test`; do ./node_modules/.bin/sardines "test-web/$$F" -o "test-web/$$F" -p browser; done